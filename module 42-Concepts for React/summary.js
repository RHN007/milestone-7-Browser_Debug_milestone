/* 

1.1 variable (let , const ) three types of variable
1.2 condition (<, > , === , !== , <= , >= ) && || , if-else ,
1.3 array : declare , lenght , index, push , pop , indexOf, icludes
1.4 for loop , while 
1.5 function return parametner 
1.6 Object property , including array , object 

--------------------

ES6 
1. template sting ${} 
2. spread (...)
2.1 copy an array then add a new element to an array 
2.1 use filter to remove an element from an array  
3. Arrow function 
3.1 No parameter (); 
3.2  sigle parameter 
3,3 multiple parameters ()
3.4 multi line 
4 destructuring : object destructuring array destructuring 
5. Object shorthand 
6. Function parameter default value ; 
7. Optional Chaining ?.
---------------

Browser API 

1. local storage , session stroages, 
2. location API ; 
3. History Api ; 
4. fetch ; 

others : 
1. array : Map , foreach ,, filter, find 
2 . ternary Operator ; 
3. Logical  and logical or && || 
4. JSON (Stingify, parse)
5 + , '' (Conversion)

*/

const x = 50;
const y = 100;
const obj = { x: x, y: y };

//shorcut
const obj1 = { x, y };
const name = "Solimullah";
const area = "Dhaka";
const nobab = {
    name: name,
    area: area,
};